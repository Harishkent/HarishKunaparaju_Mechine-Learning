head(Sales) # to get data of first 5 coloums
tail(Sales) # to get data of last 5 coloums
summary(Sales) # to get median,mean, and max values
attach(Sales) # to display the variables.
plot(Radio,Newspaper,main = "scattlerpolot") # to represent the scattlerplot by using plot command.
